# Alpha TODO

Archived after the alpha roadmap was completed and the toolkit had shipped through `0.14.0`.

| Priority | Status | Size | Published In | Area                        | Task                                                                                                                                                                                                | Why                                                                                                                                                                                                        |
| -------- | ------ | ---- | ------------ | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1       | Done   | L    | 0.1.0        | Architecture / Tooling      | Evaluate migrating the toolchain from Bun to Vite+, ideally using `vp migrate`, and consolidate dev, test, lint, format, build, and task workflows behind the Vite+ toolchain if it proves cleaner. | This is a cross-cutting stack decision that would affect local development, CI, publishing, and future library or CLI build paths, so it is best decided early rather than after more tooling accumulates. |
| P1       | Done   | M    | 0.2.0        | Architecture / Commands     | Split `src/cli.ts` into command modules and a small command registry so feature work no longer piles into one file.                                                                                 | This is the cleanest way to keep adding auth and meeting subcommands without turning the CLI into a tangle.                                                                                                |
| P1       | Done   | L    | 0.3.0        | Architecture / Client       | Introduce clear boundaries for credential source, token store, token provider/refresh, HTTP client, and Granola API client.                                                                         | Auth, 401 refresh, desktop credential import, and richer API commands all get easier once these responsibilities are separated.                                                                            |
| P1       | Done   | M    | 0.4.0        | Architecture / Rendering    | Separate data retrieval from rendering so commands return structured data first and then choose Markdown, text, JSON, YAML, or raw structured output renderers.                                     | This merges the old output-format idea with a cleaner architecture and makes the toolkit more scriptable.                                                                                                  |
| P1       | Done   | L    | 0.5.0        | Architecture / Export State | Add an export manifest/state layer to track document ID to filename, content hash, last export time, and rename/delete handling.                                                                    | This merges the incremental-write and stable-filename ideas into one foundation that will prevent churn and stale files.                                                                                   |
| P2       | Done   | S    | 0.6.0        | Config / CLI                | Fix default-path handling so missing `supabase.json` and cache files produce clean, explicit errors instead of falling through to raw `ENOENT` failures.                                            | This is the first thing users will hit on a fresh machine, and the current behaviour is unnecessarily rough.                                                                                               |
| P2       | Done   | S    | 0.7.0        | Transcripts                 | Sort transcript segments deterministically and filter or collapse non-final/interim segments before rendering.                                                                                      | Real cache data may arrive out of order or contain partial lines, which would degrade output correctness.                                                                                                  |
| P2       | Done   | L    | 0.9.0        | Auth / Sessions             | Add `auth login`, `auth status`, and `auth logout`, importing credentials from the Granola desktop app, storing them securely, and refreshing tokens automatically when possible.                   | This removes repeated `--supabase` friction, handles expiring tokens more gracefully, and unlocks richer API-driven commands beyond bulk export.                                                           |
| P2       | Done   | M    | 0.8.0        | Testing                     | Add fixture-based end-to-end tests using captured real Granola API and cache payloads, including auth-related response shapes where relevant.                                                       | Unit tests cover shapes we invented; real payload fixtures are the fastest way to catch schema mismatches.                                                                                                 |
| P3       | Done   | L    | 0.10.0       | Meeting Toolkit             | Add selective meeting-level commands such as `meeting list`, `meeting view`, and single-meeting export or inspection flows, with filtering where the API supports it.                               | The older project shows that once auth exists, browsing and targeting individual meetings is the natural step from exporter to toolkit.                                                                    |
| P3       | Done   | M    | 0.11.0       | Notes Export                | Replace the regex-based HTML fallback with a real HTML-to-Markdown conversion path.                                                                                                                 | The current fallback is acceptable for simple content but will lose fidelity on richer note structures.                                                                                                    |
| P3       | Done   | M    | 0.12.0       | API Client                  | Harden API fetching with retry/backoff handling for transient failures and rate limits, reduce reliance on a hardcoded client version, and support cleaner 401 refresh/retry plumbing.              | Network and upstream API instability are realistic failure modes for a tool like this, especially once authenticated sessions live longer.                                                                 |
| P4       | Done   | L    | 0.13.0       | ProseMirror                 | Expand the ProseMirror renderer to support more node and mark types, list metadata, escaping rules, and other richer content forms.                                                                 | This is the biggest fidelity gap for notes export once real-world documents get more complex.                                                                                                              |
